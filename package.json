{
  "name": "cafebot",
  "version": "0.1.0",
  "private": true,
  "description": "Cafebot — cafe ordering assistant: Node backend, static customer chat, static staff dashboard.",
  "scripts": {
    "start": "npm --prefix backend start",
    "postinstall": "npm --prefix backend install"
  },
  "engines": {
    "node": ">=18"
  }
}
